name: CI (Fast - Unit Tests)
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
      - run: pip install -r requirements/dev.txt
      - run: bash scripts/run_tests.sh --unit-only
